let n = parseInt(prompt("Ingrese un número"));
let contador = 0;
let cuadrados =0;
    while (contador<n) {
        contador++
        cuadrados = contador**2;
    
    }
console.log("la suma de los cuadrados es",cuadrados);